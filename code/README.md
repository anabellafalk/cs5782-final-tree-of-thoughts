# Code

This directory contains the runnable implementation for the project:

- `tot/` contains task, prompt, provider-client, and search code.
- `scripts/` contains experiment runners.
- `tests/` contains offline validation tests.
